{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build:ui": {
      "outputs": ["ui/dist/**"]
    },
    "build:ext": {
      "outputs": ["dist/**"]
    },
    "build:mcp": {
      "outputs": ["mcp/dist/**"]
    },
    "build:web": {
      "outputs": ["web/dist/**"]
    },
    "build": {
      "dependsOn": ["build:ext", "build:ui", "build:mcp", "build:web"]
    },
    "package": {
      "dependsOn": ["build:ext", "build:ui", "build:mcp"],
      "outputs": ["*.vsix"]
    }
  }
}
